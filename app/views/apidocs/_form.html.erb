<%= form_for(apidoc) do |f| %>
  <% if apidoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apidoc.errors.count, "error") %> prohibited this apidoc from being saved:</h2>

      <ul>
      <% apidoc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
